/** @format */export const CITIES = ["Pétion-Ville", "Delmas", "Carrefour", "Meyotte", "Girado"];export const SORT_LABELS: Record<SortOption, string> = {	closest: "Le plus proche",	topRated: "Le mieux noté",	mostTrips: "Le plus de courses réussies"};export const SORT_OPTIONS = Object.values(SORT_LABELS);export const SORT_BY_LABEL: Record<string, SortOption> = Object.fromEntries(	Object.entries(SORT_LABELS).map(([key, label]) => [label, key as SortOption]));